// @flow
/**
 * Block for creating autocomplete fields with local
 * and remote search and items preloading.
 */
import Logic from './Logic';
import { createModel } from './Model';

export default { Logic, createModel };
export { Logic, createModel };
export type { Model } from './Model';
